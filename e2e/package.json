{
  "name": "@party/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "STUB ONLY — owned by A11 (Wave 1). Real end-to-end coverage (join a lobby, play a full game in a browser) lands in later waves. For now this is a single trivial Playwright test so 'just e2e' / CI's e2e step has something real to run.",
  "scripts": {
    "e2e": "playwright test"
  },
  "engines": {
    "node": ">=26"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.2"
  }
}
