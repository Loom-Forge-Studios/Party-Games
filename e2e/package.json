{
  "name": "@party/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Real end-to-end coverage — owned by A11. Spawns the real server + client as child processes (see playwright.config.ts) and drives multiple real BrowserContexts against them: a full Checkers game to game.over, a host kick, a mid-game reload/resume, and a below-minimum room.start rejection.",
  "scripts": {
    "e2e": "playwright test"
  },
  "engines": {
    "node": ">=26"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.2",
    "@party/game-checkers": "*",
    "@party/protocol": "*"
  }
}
