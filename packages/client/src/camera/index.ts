// STUB ONLY — owned by A6 (Wave 1).
//
// Will hold: the CameraDirector — consumes FocusHint (@party/engine, via
// @party/protocol) emitted on GameEvents, moves the camera to follow the
// action, and returns to the local player's seat afterward. Must know
// nothing about any specific game — see docs/ARCHITECTURE.md
// "The GameEvent stream" for why this decoupling matters.

export const CAMERA_STUB = true;
