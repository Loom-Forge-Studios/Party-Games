// STUB ONLY — owned by A4 (Wave 1).
//
// Will hold: app bootstrap — username entry, WebSocket connection to the
// server, lobby create/join UI wiring, ClientMessage/ServerMessage
// (@party/protocol) send/receive, and mounting the active game's
// GamePresenter (see docs/ARCHITECTURE.md "presenter contract") once a
// room enters 'playing' phase.

export const APP_STUB = true;
