// STUB ONLY — owned by A5 (Wave 1).
//
// Will hold: the shared 3D table scene — Three.js scene/renderer bootstrap,
// seat layout (world transforms around the table, indexed by seat number),
// and the PresenterCtx (see docs/ARCHITECTURE.md) that per-game presenters
// mount into. Table code must never implement game rules — see the
// "rules/presenter split" invariant in docs/ARCHITECTURE.md.

export const TABLE_STUB = true;
