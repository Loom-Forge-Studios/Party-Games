// STUB ONLY — owned by A4 (Wave 1).
//
// Will hold: 2D/DOM UI overlay — lobby screen (room code, player list, host
// controls), HUD, chat panel. Renders RoomState (@party/protocol), never
// game rules.

export const UI_STUB = true;
